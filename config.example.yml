# 1NCE SIM Usage Checker – Organisation Configuration
#
# Copy this file to config.yml and fill in your organisation details.
# config.yml is gitignored – credentials are never committed.
#
# You can also manage organisations through the web UI (Add / Edit buttons).
# The UI never sends passwords to the browser; they stay server-side only.

organizations:
  - id: "abc12345"
    name: "ACME GmbH"
    customer_number: "10012345"
    username: "admin@acme.example.com"
    password: "super-secret-password"

  - id: "def67890"
    name: "Second Org"
    customer_number: "20067890"
    username: "admin@second.example.com"
    password: "another-secret"

# ---------------------------------------------------------------------------
# Metabase enrichment (optional)
#
# When configured, the app automatically enriches SIMs with no data or low
# data (<10 MB) with Advizeo infrastructure and admin links after each check.
# Set public_url to the .csv URL of the Metabase public question that accepts
# IMSI values as repeated ?imsi= query parameters.
# ---------------------------------------------------------------------------
# metabase:
#   public_url: https://metabase.example.com/public/question/QUESTION-UUID.csv
